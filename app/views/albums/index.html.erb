<% @albums.each do |album| %>
  <%= link_to theme_album_path(album) do %>
    <%= album.title %>
    <%= album.description %>
    <%= cl_image_tag theme.photo, height: 350, width: 450, crop: :fill, class: "img-rounded img-responsive img-show", alt: "Not Image"%>
  <% end %>
<% end %>
